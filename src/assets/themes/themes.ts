import { colors as C } from '../colors';

export const defaultLight = {
    name:      'defaultLight',
    main:      C.light,
    secondary: C.lightGrey,
};

export const dark = {
    name:      'dark',
    main:      C.darkGrey,
    secondary: C.light2,
};
